<flynt-component load:on="idle" name="NavigationMainLeft" class="w-1/3 flex justify-start items-center">
  <nav class="navigation z-[99]" aria-label="{{ labels.ariaLabel|e }}">
    <div class="flex flex-row items-center justify-between gap-sm h-full">
      <div class="flex flex-row justify-between gap-lg items-center w-full">
      {% if menu %}
        {% include 'Partials/_menu.twig' with { 'items': menu.items } only %}
      {% endif %}
      </div>
    </div>
  </nav>
</flynt-component>
