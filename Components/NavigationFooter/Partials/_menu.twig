<ul class="menu menu--level-{{ level }}">
  {% for item in items %}
    <li class="{{ item.classes | join(' ') }} text-menu text-white font-extrabold">
      <a class="menu-link text-menu text-white font-extrabold" href="{{ item.link }}"{% if item.target %} target="{{ item.target }}"{% endif %}>{{ item.title }}</a>
      {% if item.children and level + 1 <= maxLevel %}
        {% include 'Partials/_menu.twig' with { 'items': item.children, 'level': level + 1, 'maxLevel': maxLevel } only %}
      {% endif %}
    </li>
  {% endfor %}
</ul>
